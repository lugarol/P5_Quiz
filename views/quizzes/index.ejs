<h1>Lista de quizzes:</h1>

<table>
    <% for (let i in quizzes) { %>
        <% let quiz = quizzes[i]; %>
        <tr>
            <td>
                <%= quiz.question%>
            </td>
        </tr>
    <% } %>
</table>
